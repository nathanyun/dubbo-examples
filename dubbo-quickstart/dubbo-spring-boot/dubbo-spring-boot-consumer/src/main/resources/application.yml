dubbo:
  application:
    name: springboot-dubbo-zk-consumer
  protocol:
    name: dubbo
    port: -1
  registry:
    address: zookeeper://${zookeeper.address:127.0.0.1}:2181
  consumer:
    check: false
server:
  port: 8802